# Focused encoding review — candidate `45e9770`

**Decision: revise.** The explicit vertex arrays repair the round 002 size-bound gap, and the mathematical gadget and recovery argument remain supported by the initial review. The current executable map nevertheless fails on legal, sufficiently large thresholds. Fix that CLI boundary and rerun the targeted check; the unchanged gadget proof and literature review need no restart.

## Correctness

The [contract](../../work/contract.md#L3) now requires source and target arrays listing every vertex. [Forward construction](../../work/algorithm.py#L7) writes the exact target list `0..n+m-1` in the main case and exact lists for the negative-threshold triangle and trivial-size singleton. Since a legal source explicitly lists `n` vertices, `n` is bounded by input length even for an edgeless graph. Simplicity, positive target thresholds, and the `O(n² log n + bitlength(k))` output bound at [proof lines 68–72](../../work/proof.md#L68) now follow for all three branches. Decimal JSON digit count is within a constant factor of binary bit length. Recovery still reads the parent array and sorts original-vertex depths; its polynomial bound and all-witness argument are unchanged. The initial review's independent clique charging proof, special-case analysis, and target-output semantics remain applicable. The amended [prepared check](../../work/check.py#L261) and [separate verifier](../../work/verify.py#L61) assert the new target array; [verification](../../work/verification.md) records the rerun counts. I did not repeat those full suites.

The executable map is incomplete over the stated legal input set. [Contract line 4](../../work/contract.md#L4) admits every integer `k`, and [algorithm lines 38–41](../../work/algorithm.py#L38) use Python's default JSON integer parser and serializer. With source graph `n=2`, vertices `[0,1]`, no edges, and `k` equal to 4,300 nines, the main formula adds `C_2=1`, making the output threshold 4,301 digits; `json.dump` raises `ValueError` under the runtime's 4,300-digit conversion cap. With 4,301 input digits, `json.load` raises before `forward` runs. Both sources are legal and small in the complexity sense. Thus `F` is not currently a total executable map on legal sources, despite the valid abstract polynomial bound. The focused [independent check](check_encoding.py) passes the explicit-vertex cases and fails on both legal thresholds with those exact errors under `uv run --locked python` on 2026-09-23. Disable the Python digit cap for this CLI or use an integer I/O path that handles arbitrarily long legal thresholds, then show the check passing. The same issue applies to negative thresholds before the special branch can execute.

No change to the actual charging argument or `G` is needed for this finding. [Proof lines 35–55](../../work/proof.md#L35) still establish the lower bound for every valid target tree; the canonical parent-array representation in the [contract](../../work/contract.md#L17) lets the implementation recover the depth order. The finite injected-instance evidence remains useful for the changed serialization, but cannot cover the unbounded integer domain.

## Novelty

The changed files add an explicit graph encoding, not a new mathematical construction. I reuse the [initial review's primary-source assessment](../initial/review.md#novelty), searched on 2026-09-23: Garey–Johnson, GT45, already attributes a Linear Arrangement to Rooted Tree Arrangement transformation to Gavril 1977; the upstream issue does not give a gadget or decoder; and the cited 1996 rooted-tree section does not supply this construction. Gavril's original proof was unavailable, so whether this exact gadget and recovery were published remains unresolved. The candidate may claim a newly derived proof in this campaign, not established global originality. The encoding repair does not change that judgment.

## Significance

The fixed question permits a complete reconstruction. Once the executable integer boundary is repaired, the clique and edge-vertex construction still supplies the concrete instance map, all-output recovery, and general proof absent from the upstream rule issue, as assessed in the [initial review](../initial/review.md#significance). The dominant `Theta(n²)` clique edges and up to `Theta(n²)` explicitly listed target vertices remain polynomial. Dense targets may be expensive for solvers; the small checks do not support practical-scale speed claims. Significance remains supported, conditional on total executable construction.

## Isolation and model route

This was a fresh reviewer subagent task at commit `45e9770`; I read the current files and previous review rather than receiving the proposer's private reasoning. The registered reviewer instructions and research-review skill imposed the write boundary as instructions. The actual tool surface exposed agent spawning, with no enforced depth cap or tool denial. Filesystem mode was `danger-full-access` with no write sandbox; approval policy was `never`. I obeyed the assigned directory boundary, but it was not enforced by tooling. The route exposed to me was **Codex GPT-6**; the exact variant and proposer's model route were not exposed. Neither route separation nor process isolation substitutes for the evidence above.
