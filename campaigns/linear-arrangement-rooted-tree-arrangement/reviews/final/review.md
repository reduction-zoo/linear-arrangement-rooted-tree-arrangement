# Final focused review — candidate `6fd592a`

**Decision: advance to expert review.** The integer-conversion failure identified at `45e9770` is repaired in the current executable CLI. The explicit-vertex repair, clique charging proof, all-output recovery, and fixed-question significance remain supported. This decision does not establish that the particular gadget was unpublished.

## Correctness

The [contract](../../work/contract.md#L3) admits thresholds of arbitrary finite length in decimal JSON. In the current [CLI](../../work/algorithm.py#L37), `sys.set_int_max_str_digits(0)` executes before `json.load`, `forward` or `extract`, and `json.dump`. The project requires Python 3.12 or later, where this API is available. Thus Python's default 4,300-digit parse and formatting limit no longer cuts down the legal source domain. Decimal digits and binary bit length differ by a constant factor, and parsing, addition, and formatting finite integers are polynomial in their encoded lengths. The [proof's size argument](../../work/proof.md#L68) still applies: explicit source vertices bound `n` by input length, while `F` lists `O(n²)` target vertices and edges and writes a threshold with `O(bitlength(k)+log(n+1))` bits. `G` reads the same unrestricted source JSON and writes an `O(n log n)`-bit order or `NO-SOLUTION`.

I reran the formerly failing [independent encoding check](../encoding/check_encoding.py) and the candidate's [large-integer end-to-end check](../../work/evidence/large-integers/check.py) using `uv run --locked python`; both exited zero on 2026-09-23. I also ran a separate inline subprocess check against the current `algorithm.py`: an edgeless two-vertex graph with 4,300 nines produced the exact 4,301-digit `K=k+1`; a 4,301-digit positive input produced the same exact formula; both valid target trees decoded to `[0,1]`. A 4,301-digit negative input produced the fixed triangle with `K=1`, and target `NO-SOLUTION` decoded to source `NO-SOLUTION`. These cases exercise input parsing, output formatting, threshold carry, both special and main branches, and both CLI modes. They do not replace the general proof.

The change to `algorithm.py` is the one CLI configuration call; the gadget and decoder are unchanged from the [initial review](../initial/review.md#correctness), and the explicit vertex lists are unchanged from the [encoding review](../encoding/review.md#correctness). I checked the current [proof's charging step](../../work/proof.md#L35) against the current [construction](../../work/algorithm.py#L7): the original clique forces a single ancestor chain; each gadget strictly internal to its edge endpoints can save at most one unit in its two edge distances while adding at least one unit to clique cost. Other gadgets cost at least rank distance plus two. A path with each gadget attached below its later endpoint attains the bound. Hence `T(H)=C_n+L(G)+2m`, and the depth-sorted order from every valid target witness costs at most `k`. The `k<0` triangle and `n<=1` singleton handle their legal source cases; the target always has a valid semantic output, either a tree witness or `NO-SOLUTION`, and the decoder handles both. The previous independent injected-instance suites and their counts in [verification](../../work/verification.md) are unaffected evidence; I did not repeat them merely to reproduce passing counts. I found no remaining correctness gap introduced by the repair.

## Novelty

The integer I/O change adds no mathematical claim. I reuse the [initial review's literature assessment](../initial/review.md#novelty) and checked the primary [Garey–Johnson GT45 entry](https://perso.limos.fr/~palafour/PAPERS/PDF/Garey-Johnson79.pdf) again by search on 2026-09-23. GT45 defines the same ancestor-comparability and tree-distance decision problem and explicitly says its transformation is from Optimal Linear Arrangement, citing Gavril 1977a. Thus the existence of such a transformation and the associated hardness consequence are classical. The [upstream rule issue](https://github.com/CodingThrust/problem-reductions/issues/888) does not provide this executable gadget and all-output decoder. Gavril's 1977 proof remains unavailable for direct comparison, so whether the exact clique and edge-vertex construction, offset, or decoder appeared there is unresolved. The supported originality claim is a proof newly derived in this campaign, not global novelty.

## Significance

The fixed question allows a complete reconstruction. Relative to the GT45 listing and upstream rule issue, the deliverable is a specified instance map, executable recovery for every valid tree and `NO-SOLUTION`, and a general charging proof. The integer repair makes this deliverable apply to the entire stated threshold encoding, including arbitrarily long legal inputs. The dominant overhead remains the `Theta(n²)` clique edges, up to `Theta(n²)` listed target vertices, and a `Theta(n³)` additive baseline. The verified instances are small; there is no evidence for practical large-scale solver performance. This limitation does not violate a question-specific resource requirement.

## Isolation and model route

This was a fresh-context reviewer subagent task with the candidate commit and artifact paths supplied by the parent. I read the current files and prior reviews, without the proposer's private reasoning. The registered Codex reviewer instructions and research-review skill were in force as instructions. Agent spawning was still exposed, with no enforced tool denial or delegation depth cap; I did not use it. Filesystem mode was `danger-full-access`, with no sandbox restricting writes to this directory, and approval policy was `never`. I wrote only this assigned review file. The exposed route was **Codex GPT-6**; neither an exact model variant nor the proposer's route was exposed, so I claim no cross-model independence. Process isolation and the explicit write instruction do not substitute for the proof and checks above.
